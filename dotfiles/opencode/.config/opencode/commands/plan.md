---
description: Write or update the roadmap in ROADMAP.md
agent: plan
---
Roadmap requested: $ARGUMENTS

1. Ground yourself: read the repo's package.json, existing ROADMAP.md, and `git log --oneline -10`. Don't over-explore.
2. Ask me for the project's overall direction if $ARGUMENTS doesn't give it.
3. Write/update `ROADMAP.md` at the repo root: Macro Plan (2-3 sentences), an Active card (Intent, Acceptance Criteria/DoD, Out of Scope), Next (abstract one-liners), Done (objective outcomes).
4. Cards only — no atomic task lists in ROADMAP.md.
5. Build the Todo list from the Active card. Ask me to confirm before starting implementation.
