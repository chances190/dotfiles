# Command Permission Reference

Single source of truth for opencode agent bash permissions.

## Policy (per sub-agent)

| Agent     | Read-only | Mutation | Destructive (git-safe) | Destructive |
|-----------|-----------|----------|------------------------|-------------|
| **ask**   | allow     | deny     | deny                   | deny        |
| **adhoc** | allow     | allow    | ask                    | ask         |
| **coder** | allow     | allow    | allow                  | ask         |
| **architect**  | allow     | ask      | deny                   | deny        |

## Classification

- **read-only** — reads state only.
- **mutation** — changes state in a temporary, self-contained, or easily reversible way.
- **destructive** — changes state in a hard-to-reverse way OR can lead to any kind of data loss (deleting anything at all, installing into non-self-contained places, moving many things, etc.).
- **destructive (git-safe)** — destructive, but inside a git repo reversible via checkout/reset/stash (deleting files, staging, moving files, stash push, …).
- Building, formatting, installing and running tests are **not** destructive — their effects are regenerable or easily reversible → **mutation**.
- Operations **on git itself** that change repo state and are hard to reverse are destructive, **not** git-safe (commit, merge, rebase, changing upstream, …).
- Not 100% sure about a row? It's flagged in the Notes column — verify before relying on it.

## Rules Matrix

Commands are ordered alphabetically. Subcomands are ordered based on specificity of glob pattern (general before specific). 

Commands tagged as rtk=y are rewritten by the Rust Token Killer hook and must be prepended with `rtk <command>`. RTK rewrites commands before the permission check, so bash rules on the agent's configuration must match the `rtk ...` form: https://github.com/anomalyco/opencode/issues/35882

| Command (glob)                  | rtk | Class                  | ntes                                                                                                                                                            |
|---------------------------------|-----|------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `apt *`                         | n   | destructive            | system package manager; installs to non-self-contained locations                                                                                                 |
| `bun build*`                    | n   | mutation               | bundler; regenerable output                                                                                                                                      |
| `bun install*`                  | n   | mutation               | nde_modules is self-contained; lockfile diff reversible via git                                                                                                 |
| `bun run*`                      | n   | mutation               | runs package scripts — impact depends on the script                                                                                                              |
| `bun test*`                     | n   | mutation               | test runner                                                                                                                                                      |
| `cargo build*`                  | y   | mutation               | build output in `target/` (usually gitignored); delete/regenerate to undo                                                                                        |
| `chmod *`                       | n   | mutation               | reversible (chmod back); can break perms/executability if wrong                                                                                                  |
| `chown *`                       | n   | destructive            | needs root to reverse                                                                                                                                            |
| `cp *`                          | n   | destructive (git-safe) | clobbers destination → data loss; git-safe if tracked                                                                                                            |
| `curl*`                         | y   | read-only              | GET/HEAD; mutating `-X`/`--request` forms below                                                                                                                  |
| `curl --request*`               | y   | destructive            | any HTTP method incl. mutations                                                                                                                                  |
| `curl -X DELETE*`               | y   | destructive            |                                                                                                                                                                  |
| `curl -X POST*`                 | y   | destructive            |                                                                                                                                                                  |
| `curl -X PUT*`                  | y   | destructive            |                                                                                                                                                                  |
| `df*`                           | y   | read-only              |                                                                                                                                                                  |
| `docker compose*`               | n   | destructive            | lifecycle mgmt; `down`/`rm -v` removes containers, networks, volumes                                                                                             |
| `docker exec*`                  | y   | mutation               | runs arbitrary cmds in container; state self-contained (recreate to reset)                                                                                       |
| `docker images*`                | y   | read-only              |                                                                                                                                                                  |
| `docker inspect*`               | n   | read-only              |                                                                                                                                                                  |
| `docker logs*`                  | y   | read-only              |                                                                                                                                                                  |
| `docker ps*`                    | y   | read-only              |                                                                                                                                                                  |
| `docker rm*`                    | n   | destructive            | deletes container + write layer; volumes may persist                                                                                                             |
| `docker rmi*`                   | n   | destructive            | deletes image                                                                                                                                                    |
| `docker stop*`                  | n   | destructive            | interrupts container; reversible via start                                                                                                                       |
| `du*`                           | y   | read-only              |                                                                                                                                                                  |
| `env`                           | n   | read-only              |                                                                                                                                                                  |
| `fd *`                          | n   | read-only              |                                                                                                                                                                  |
| `file *`                        | n   | read-only              |                                                                                                                                                                  |
| `find*`                         | y   | read-only              |                                                                                                                                                                  |
| `free*`                         | n   | read-only              |                                                                                                                                                                  |
| `gh issue close*`               | y   | mutation               | reversible via `gh issue reopen`                                                                                                                                 |
| `gh issue list*`                | y   | read-only              |                                                                                                                                                                  |
| `gh pr list*`                   | y   | read-only              |                                                                                                                                                                  |
| `gh pr merge*`                  | y   | destructive            | merges remote PR; hard to reverse; not git-safe                                                                                                                  |
| `gh pr view*`                   | y   | read-only              |                                                                                                                                                                  |
| `git add*`                      | y   | destructive (git-safe) | staging — user example; reversible via reset                                                                                                                     |
| `git branch*`                   | y   | read-only              | listing; destructive forms below                                                                                                                                 |
| `git branch --delete*`          | y   | destructive            | deletes branch; recoverable via reflog until GC                                                                                                                  |
| `git branch --move*`            | y   | destructive            | renames branch ref; affects remote tracking                                                                                                                      |
| `git branch --set-upstream-to*` | y   | destructive            | changing upstream — user example                                                                                                                                 |
| `git branch -D*`                | y   | destructive            | force-deletes unmerged branch                                                                                                                                    |
| `git branch -d*`                | y   | destructive            | deletes merged branch                                                                                                                                            |
| `git branch -m*`                | y   | destructive            | renames branch                                                                                                                                                   |
| `git checkout*`                 | y   | mutation               | branch switch/create; note: plain `git checkout <path>` restores files (destructive) — only `--`/`.`/`-f` gated                                                  |
| `git checkout -- *`             | y   | destructive            | discards uncommitted changes; not recoverable                                                                                                                    |
| `git checkout -b*`              | y   | mutation               | create branch (redundant with `git checkout*`)                                                                                                                   |
| `git checkout -f*`              | y   | destructive            | force discard                                                                                                                                                    |
| `git checkout .*`               | y   | destructive            | discards all changes in cwd                                                                                                                                      |
| `git cherry-pick*`              | n   | destructive            | applies commits, creates history                                                                                                                                 |
| `git clean*`                    | n   | destructive            | deletes untracked files — NOT git-safe                                                                                                                           |
| `git clone*`                    | n   | mutation               | creates new dir; undo via rm                                                                                                                                     |
| `git commit*`                   | y   | destructive            | user example                                                                                                                                                     |
| `git diff*`                     | y   | read-only              |                                                                                                                                                                  |
| `git fetch*`                    | y   | mutation               | updates remote-tracking refs in .git                                                                                                                             |
| `git log*`                      | y   | read-only              |                                                                                                                                                                  |
| `git merge*`                    | n   | destructive            | user example                                                                                                                                                     |
| `git pull*`                     | y   | destructive            | can auto-merge/rebase                                                                                                                                            |
| `git push*`                     | y   | destructive            | publishes to remote                                                                                                                                              |
| `git rebase*`                   | n   | destructive            | rewrites history                                                                                                                                                 |
| `git reset*`                    | n   | destructive            | moves refs; can discard work                                                                                                                                     |
| `git restore*`                  | n   | destructive            | discards changes                                                                                                                                                 |
| `git revert*`                   | n   | destructive            | creates reverse commits                                                                                                                                          |
| `git rm*`                       | n   | destructive (git-safe) | tracked files only; restore via checkout — git-safe, but still gated on build                                                                                    |
| `git show*`                     | y   | read-only              |                                                                                                                                                                  |
| `git stash*`                    | y   | read-only              | list/show; BEWARE bare `git stash` = push (destructive); forms below                                                                                             |
| `git stash apply*`              | y   | destructive (git-safe) | writes working tree; reversible via checkout                                                                                                                     |
| `git stash branch*`             | y   | destructive (git-safe) | creates branch + drops stash                                                                                                                                     |
| `git stash clear*`              | y   | destructive            | recoverable via fsck, hard                                                                                                                                       |
| `git stash drop*`               | y   | destructive            | recoverable via fsck, hard                                                                                                                                       |
| `git stash pop*`                | y   | destructive (git-safe) | applies + drops; conflict keeps the stash                                                                                                                        |
| `git stash push*`               | y   | destructive (git-safe) | pushing to stash — user example; reversible via pop                                                                                                              |
| `git stash save*`               | y   | destructive (git-safe) |                                                                                                                                                                  |
| `git status*`                   | y   | read-only              |                                                                                                                                                                  |
| `git switch*`                   | n   | mutation               | branch switch; refuses to lose changes                                                                                                                           |
| `git tag*`                      | n   | mutation               | create tag; destructive forms below                                                                                                                              |
| `git tag --delete*`             | n   | destructive            | tags have no reflog — hard to recover                                                                                                                            |
| `git tag -d*`                   | n   | destructive            |                                                                                                                                                                  |
| `git worktree*`                 | y   | read-only              | list; forms below                                                                                                                                                |
| `git worktree add*`             | y   | mutation               | creates worktree; undo via remove                                                                                                                                |
| `git worktree remove*`          | y   | destructive            | removes checkout dir; branch/commits stay in repo                                                                                                                |
| `go test*`                      | y   | mutation               | writes test cache                                                                                                                                                |
| `gradlew*`                      | y   | mutation               | gradle build wrapper; output in `build/` (regenerable)                                                                                                           |
| `grep*`                         | y   | read-only              |                                                                                                                                                                  |
| `jest*`                         | y   | mutation               | test runner; writes cache/report                                                                                                                                 |
| `journalctl*`                   | n   | read-only              |                                                                                                                                                                  |
| `jq*`                           | y   | read-only              |                                                                                                                                                                  |
| `kill *`                        | n   | destructive            | terminates processes; unsaved state lost                                                                                                                         |
| `killall *`                     | n   | destructive            |                                                                                                                                                                  |
| `kubectl apply*`                | y   | destructive            | declarative; reversible via git/delete if GitOps, else manual                                                                                                    |
| `kubectl delete*`               | n   | destructive            | deletes cluster resources; hard to reverse                                                                                                                       |
| `kubectl get*`                  | y   | read-only              |                                                                                                                                                                  |
| `lint*`                         | y   | mutation               | also matches `eslint` (rewrites to `rtk lint`); `--fix` mutates (git-safe)                                                                                       |
| `ls*`                           | y   | read-only              |                                                                                                                                                                  |
| `lsof*`                         | n   | read-only              |                                                                                                                                                                  |
| `make*`                         | y   | mutation               | runs Makefile targets — impact depends on target; typical `make` builds (regenerable output)                                                                     |
| `make install*`                 | y   | destructive            | common target writes to system paths (non-self-contained); NOT git-safe                                                                                          |
| `mkdir *`                       | n   | mutation               | undo via rmdir                                                                                                                                                   |
| `mount *`                       | n   | destructive            | system mount changes; umount reverses                                                                                                                            |
| `mv *`                          | n   | destructive (git-safe) | moving things — user example; clobbers destination                                                                                                               |
| `mvn*`                          | y   | mutation               | maven build; output in `target/` (regenerable)                                                                                                                   |
| `next*`                         | y   | mutation               | `next build` writes `.next/` (regenerable); `next dev` runs a server; rewrite drops subcommand (`rtk next`)                                                      |
| `npm install*`                  | n   | mutation               | nde_modules is self-contained; lockfile diff reversible via git                                                                                                 |
| `npm run*`                      | y   | mutation               | runs package scripts — impact depends on the script; typical build scripts produce regenerable output                                                            |
| `pacman *`                      | n   | destructive            | system packages, non-self-contained                                                                                                                              |
| `paru *`                        | n   | destructive            | AUR helper; builds + installs system packages                                                                                                                    |
| `ping*`                         | y   | read-only              |                                                                                                                                                                  |
| `pip install*`                  | n   | destructive            | global site-packages = non-self-contained; venv = mutation                                                                                                       |
| `pkill *`                       | n   | destructive            |                                                                                                                                                                  |
| `podman compose*`               | n   | destructive            | lifecycle mgmt; `down`/`rm -v` removes containers, networks, volumes; not hooked — passthrough                                                                   |
| `podman images*`                | n   | read-only              | n podman hook at all                                                                                                                                            |
| `podman logs*`                  | n   | read-only              |                                                                                                                                                                  |
| `podman ps*`                    | n   | read-only              |                                                                                                                                                                  |
| `podman rm*`                    | n   | destructive            |                                                                                                                                                                  |
| `podman run*`                   | n   | destructive            | runs arbitrary containers/processes                                                                                                                              |
| `podman stop*`                  | n   | destructive            |                                                                                                                                                                  |
| `prettier*`                     | y   | mutation               | formatting; `--write` rewrites source in place but is reversible via checkout — the borderline case, flagged                                                     |
| `ps*`                           | y   | read-only              |                                                                                                                                                                  |
| `pwd`                           | n   | read-only              |                                                                                                                                                                  |
| `pytest*`                       | y   | mutation               | test runner                                                                                                                                                      |
| `read*`                         | y   | read-only              | cat, head, tail all rewrite here                                                                                                                                 |
| `rg*`                           | y   | read-only              |                                                                                                                                                                  |
| `rm *`                          | n   | destructive (git-safe) | deleting files — user example; git-safe if tracked                                                                                                               |
| `rm -rf*`                       | n   | destructive (git-safe) | order AFTER `rm *`; irreversible outside git                                                                                                                     |
| `rmdir *`                       | n   | destructive            | removes empty dirs                                                                                                                                               |
| `rsync*`                        | y   | destructive            | can overwrite/delete (`--delete`) destination                                                                                                                    |
| `rtk meta`                      | —   | read-only              | rtk meta group (discover, session, telemetry, learn, verify, hook-audit, rewrite, config, gain, cc-economics, smart, summary); informational — never allowlisted |
| `rtk pipe*`                     | —   | mutation               | pipes commands                                                                                                                                                   |
| `rtk proxy*`                    | —   | mutation               | pass-through pipe, runs arbitrary cmds; removed from build by user 2026-08-05 → `*: allow`                                                                       |
| `rtk run *`                     | —   | destructive            | arbitrary exec                                                                                                                                                   |
| `rtk trust*`                    | —   | mutation               | enables project TOML filters; reversible via untrust; removed from build by user 2026-08-05 → `*: allow`                                                         |
| `rtk untrust*`                  | —   | mutation               | removes project filter trust; removed from build by user 2026-08-05 → `*: allow`                                                                                 |
| `ruff*`                         | p   | mutation               | `ruff check*` and `ruff format*` → `rtk ruff ...`; bare `ruff` and `ruff --fix*` pass through raw (verified 2026-08-05)                                          |
| `scp *`                         | n   | destructive            | overwrites remote files                                                                                                                                          |
| `ss*`                           | n   | read-only              |                                                                                                                                                                  |
| `ssh*`                          | y   | destructive            | runs arbitrary commands remotely                                                                                                                                 |
| `stat*`                         | y   | read-only              |                                                                                                                                                                  |
| `sudo *`                        | n   | destructive            | root escalation — arbitrary impact                                                                                                                               |
| `systemctl disable*`            | n   | mutation               | persistent boot config; reversible via enable                                                                                                                    |
| `systemctl enable*`             | n   | mutation               | persistent boot config; reversible via disable                                                                                                                   |
| `systemctl reload*`             | n   | mutation               | graceful config reload                                                                                                                                           |
| `systemctl restart*`            | n   | destructive            | interrupts service (brief downtime)                                                                                                                              |
| `systemctl start*`              | n   | mutation               | reversible via stop                                                                                                                                              |
| `systemctl status*`             | y   | read-only              | only `status` hooks; other subcommands raw                                                                                                                       |
| `systemctl stop*`               | n   | destructive            | interrupts service; potential data loss; reversible via start                                                                                                    |
| `touch *`                       | n   | mutation               | creates/updates timestamps                                                                                                                                       |
| `tree*`                         | y   | read-only              |                                                                                                                                                                  |
| `tsc*`                          | y   | mutation               | typecheck/compile; `--noEmit` read-only, emit writes regenerable output                                                                                          |
| `umount *`                      | n   | destructive            | can interrupt active I/O                                                                                                                                         |
| `uname*`                        | n   | read-only              |                                                                                                                                                                  |
| `uptime`                        | n   | read-only              |                                                                                                                                                                  |
| `uv run*`                       | n   | mutation               | rtk wraps the inner cmd (`uv run pytest` → `uv run rtk pytest`); raw prefix still matches                                                                        |
| `vitest*`                       | y   | mutation               | test runner; rewrite drops `run` (`rtk vitest`)                                                                                                                  |
| `wc*`                           | y   | read-only              |                                                                                                                                                                  |
| `wget*`                         | y   | mutation               | downloads; appends `.1` on collision, rarely overwrites                                                                                                          |
| `which *`                       | n   | read-only              |                                                                                                                                                                  |
| `yay *`                         | n   | destructive            | AUR helper; builds + installs system packages                                                                                                                    |