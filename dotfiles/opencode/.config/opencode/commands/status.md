---
description: Re-read ROADMAP.md and git state, rebuild the Todo list
---
1. Read `ROADMAP.md` at the repo root. If missing, say so.
2. Inspect git state: current branch, `git status`, and `git diff` against the last commit.
3. Rebuild the Todo list for the current milestone from the roadmap + git state; mark anything already done.
4. Report: current milestone, what git shows in flight, next actions. Do not start new work.
